# ============================================================
# 不同模型的kv分析 - 上传规则
# 上传：实验脚本 + 实验结果（指标 / 日志 / 图表 / 报告）
# 不上传：实验中产生的模型参数、权重、checkpoint、中间状态
# ============================================================

# ---- 模型权重 / checkpoint / 参数（一律不上传）----
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
*.onnx
*.pb
*.tflite
*.h5
*.hdf5

# ---- 中间数据 / 大型数值数组（实验中间产物，不上传）----
*.npy
*.npz

# ---- 训练/生成产物目录（整目录不上传）----
checkpoints/
saved_models/
weights/
model_cache/

# ---- Python / 运行环境 ----
__pycache__/
*.pyc
.venv/
.pytest_cache/

# ---- 系统 / 临时文件 ----
.DS_Store
Thumbs.db
*.tmp
*~
